#from webtest import TestApp
#import server


#def test_post_cell():
    #app = TestApp(server.app)

    #response = app.post('/api/v1/cell?worksheet=testsheet&cellRow=1&cellCol=A',
                        #{'username': 'foo',
                         #'password': 'bar',
                         #'value': 'abc'})
    #assert response.status == "200 OK"
